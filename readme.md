# Simple Lexical Analyzer in Go

This is a university project for the Compiler Design course. 
It reads C++ code from a file and identifies tokens.

## How to Run
1. Put your code in `code.txt`.
2. Run the program:
   ```bash
   go run main.go